# Compiled binary
/forge

# OS / editor noise
.DS_Store
*.swp
*~

# Future build output we might add
/dist/
/build/

# Test scaffolds (created by manual `./forge new` runs in the repo root)
/demo/
/test-*/
/*-scratch/

# Absolute-path test artifacts that leaked into the repo root
/home/
/tmp/

/demo-*/
/interactive-*/

# Scaffolded test projects from `./forge new` runs in the repo root
# Any pattern matching a kebab-case name at repo root
/demo/
/demo-*/
/interactive-*/
/test-*/
/verify-*/
/*-scratch/
/*-test/

# Analyzer runtime output (forge analyze writes here)
/.forge/
.mcp.json

# M13.0 spike: throwaway Node tooling, not part of the Go module
/spike/
